//! Local source Field executable scaffold.

use fieldnotes_field_protocol as _;
use std::process::ExitCode;

fn main() -> ExitCode {
    eprintln!("fieldnotes-field-local is a scaffold; collection is not implemented");
    ExitCode::FAILURE
}
