//! Microsoft Teams Field executable scaffold.

use fieldnotes_field_protocol as _;
use fieldnotes_msgraph as _;
use std::process::ExitCode;

fn main() -> ExitCode {
    eprintln!(
        "fieldnotes-field-teams is a scaffold; Microsoft Graph collection is not implemented"
    );
    ExitCode::FAILURE
}
