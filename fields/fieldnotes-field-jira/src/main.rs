//! Jira Field executable scaffold.

use fieldnotes_field_protocol as _;
use std::process::ExitCode;

fn main() -> ExitCode {
    eprintln!("fieldnotes-field-jira is a scaffold; Jira collection is not implemented");
    ExitCode::FAILURE
}
