[package]
name = "fieldnotes-graph"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
fieldnotes-domain.workspace = true
fieldnotes-format.workspace = true

[dev-dependencies]
# Fixed clock, deterministic byte source, and the path to the frozen A1 fixture
# corpus, so tests replay byte-identical projections over approved bytes.
fieldnotes-test-support.workspace = true

[lints]
workspace = true
